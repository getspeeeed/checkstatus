{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "scripts": {
    "log": "firebase functions:log --project tarostats",
    "deploy": "firebase deploy --project tarostats",
    "test": "mocha test/ --timeout 15000"
  },
  "dependencies": {
    "firebase-admin": "^4.1.2",
    "firebase-functions": "^0.5",
    "request": "^2.83.0"
  },
  "private": true,
  "devDependencies": {
    "chai": "^4.1.2",
    "firebase": "^4.9.1",
    "mocha": "^5.0.0",
    "sinon": "^4.2.2"
  }
}
